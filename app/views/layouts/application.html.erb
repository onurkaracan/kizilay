<!DOCTYPE html>
<html>
  <head>
    <title>Kizilay</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div><%= link_to "Donor yonetim sayfası", new_donor_path %></div>
    <div><%= link_to "add blood", new_donor_path %></div>
      <div><%= link_to "request blood", new_donor_path %></div>
        <div><%= link_to "giriş", new_donor_path %></div>
          <%= yield %>
  </body>
</html>
