<%= form_with(model: user_branch) do |form| %>
  <% if user_branch.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_branch.errors.count, "error") %> prohibited this user_branch from being saved:</h2>

      <ul>
        <% user_branch.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :branch_id, style: "display: block" %>
    <%= form.text_field :branch_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
