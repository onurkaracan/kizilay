<%= form_with(model: blood_type) do |form| %>
  <% if blood_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(blood_type.errors.count, "error") %> prohibited this blood_type from being saved:</h2>

      <ul>
        <% blood_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :type_name, style: "display: block" %>
    <%= form.text_field :type_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
